import { connect } from "react-redux";
import FeedbackHandlingPage from "./FeedbackHandlingPage";


let mapStateToProps = (state) => {
  return {
    
  }
};

let mapDispatchToProps = (dispatch) => {
  return {
  
  }
};

export const FeedbackHandlingPageContainer = connect(mapStateToProps, mapDispatchToProps)(FeedbackHandlingPage);



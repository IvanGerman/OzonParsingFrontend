import { connect } from "react-redux";
import DailySearchPage from "./DailySearchPage";


let mapStateToProps = (state) => {
  return {
    
  }
};

let mapDispatchToProps = (dispatch) => {
  return {
  
  }
};

export const DailySearchPageContainer = connect(mapStateToProps, mapDispatchToProps)(DailySearchPage);


